import { Routes } from '@angular/router';
import { CandidaturePageComponent } from './candidature-page/candidature-page.component';

export const routes: Routes = [
    {
        path : "candidature",
        component : CandidaturePageComponent
    }
];
